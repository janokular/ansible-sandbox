---
- name: Package installation
  apt:
    name: "{{ packages }}"
    state: latest
    update_cache: true

- name: Service startup
  systemd:
    name: "{{ item }}"
    state: started
    enabled: true
  loop: "{{ services }}"
